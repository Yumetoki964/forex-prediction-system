# Render.com用Dockerfile（シンプル版）
FROM python:3.11-slim

WORKDIR /app

# 最小限の依存関係をインストール
RUN pip install fastapi uvicorn

# シンプルなテストファイルをコピー
COPY simple_test.py .

# ポート設定
EXPOSE 8000

# シンプルな起動
CMD python simple_test.py